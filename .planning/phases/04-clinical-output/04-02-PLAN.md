---
phase: 04-clinical-output
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/_shared/health_annual_report.py
  - skills/health-annual-report/SKILL.md
  - scripts/generate_annual_report.py
  - tests/test_annual_report.py
autonomous: true
requirements: [ARPT-01, ARPT-02, ARPT-03]

must_haves:
  truths:
    - "User can run `python scripts/generate_annual_report.py --year 2025` and get a year-in-review report"
    - "Report includes metric trajectories, medication adherence, health events timeline, improvement areas, correlation insights, goals review"
    - "Report output is a single self-contained HTML file with inline CSS and base64-encoded chart PNGs"
    - "Report is person_id-aware: --person-id mom produces mom's report"
    - "Report degrades gracefully with sparse data (e.g., only 3 months of tracking)"
  artifacts:
    - path: "skills/_shared/health_annual_report.py"
      provides: "HealthAnnualReport class composing existing engines"
      contains: "class HealthAnnualReport"
    - path: "scripts/generate_annual_report.py"
      provides: "CLI entry point"
      contains: "argparse"
    - path: "skills/health-annual-report/SKILL.md"
      provides: "Skill definition for OpenClaw discovery"
      contains: "health-annual-report"
    - path: "tests/test_annual_report.py"
      provides: "Unit tests for annual report"
      contains: "class"
  key_links:
    - from: "health_annual_report.py"
      to: "HealthChartEngine"
      via: "generate_*_chart(days=365) for 12-month trajectories"
      pattern: "chart_engine\\.generate_"
    - from: "health_annual_report.py"
      to: "CorrelationEngine"
      via: "discover_correlations() for top 3 insights"
      pattern: "discover_correlations"
    - from: "health_annual_report.py"
      to: "CrossSkillReader"
      via: "read_medication_doses for adherence calculation"
      pattern: "reader\\.read"
    - from: "health_annual_report.py"
      to: "daily logs"
      via: "glob(year-*.md) for health events extraction"
      pattern: "daily_dir.*glob"
---

<objective>
Build the annual health report feature: a new `HealthAnnualReport` class that generates a comprehensive year-in-review HTML report with metric trajectories, medication adherence, health events timeline, improvement areas, correlation insights, and goals review.

Purpose: This is the "engagement feature" -- what makes users feel continuous tracking was worth it. "Health Wrapped" for personal health.
Output: `health_annual_report.py` module, CLI script, SKILL.md, tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clinical-output/04-CONTEXT.md
@.planning/phases/04-clinical-output/04-RESEARCH.md

@skills/_shared/health_chart_engine.py
@skills/_shared/correlation_engine.py
@skills/_shared/cross_skill_reader.py
@skills/_shared/health_memory.py
@skills/_shared/health_data_store.py
@memory/health/items/ (list directory to understand available item files)
@memory/health/daily/ (list directory to understand daily log format)

<interfaces>
<!-- Key APIs the executor needs to compose against -->

From skills/_shared/health_chart_engine.py:
```python
class HealthChartEngine:
    def __init__(self, data_dir: str | None = None, person_id: str | None = None): ...
    def generate_blood_pressure_chart(self, days: int = 30) -> str: ...
    def generate_blood_glucose_chart(self, days: int = 30) -> str: ...
    def generate_weight_chart(self, days: int = 30) -> str: ...
    def generate_sleep_chart(self, days: int = 30) -> str: ...
```

From skills/_shared/correlation_engine.py:
```python
class CorrelationEngine:
    def __init__(self, data_dir: str | None = None, now_fn=None): ...
    def discover_correlations(self, ...) -> list[CorrelationResult]: ...

class CorrelationResult:
    def to_natural_language(self) -> str: ...
    def to_dict(self) -> dict: ...
    def is_significant(self) -> bool: ...
```

From skills/_shared/cross_skill_reader.py:
```python
class CrossSkillReader:
    def __init__(self, data_dir: str | None = None): ...
    def read(self, concept: str, start=None, end=None, person_id: str | None = None) -> list[dict]: ...
    def read_medication_doses(self, start=None, end=None, person_id: str | None = None) -> list[dict]: ...
    def read_blood_pressure(self, start=None, end=None, person_id: str | None = None) -> list[dict]: ...
    # etc.
```

From skills/_shared/health_memory.py:
```python
class HealthMemoryWriter:
    def __init__(self, workspace_root=None, memory_root=None, person_id=None, ...): ...
    # Properties: daily_dir, items_dir, files_dir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HealthAnnualReport class + styled HTML template</name>
  <files>skills/_shared/health_annual_report.py, tests/test_annual_report.py</files>
  <read_first>
    - skills/_shared/health_chart_engine.py (chart generation API, constructor)
    - skills/_shared/correlation_engine.py (discover_correlations API, CorrelationResult.to_natural_language())
    - skills/_shared/cross_skill_reader.py (read_medication_doses, read_blood_pressure, etc.)
    - skills/_shared/health_memory.py (HealthMemoryWriter constructor, daily_dir, items_dir, files_dir)
    - memory/health/daily/ (ls to see daily log filename format: YYYY-MM-DD.md)
    - memory/health/items/ (ls to see item files: medications.md, goals, etc.)
    - memory/health/items/_health-profile.md (profile format with goals section)
  </read_first>
  <behavior>
    - Test 1: HealthAnnualReport.__init__ accepts data_dir, person_id, workspace_root, memory_dir, year
    - Test 2: generate() returns dict with "path" and "content" keys; content is valid HTML with DOCTYPE
    - Test 3: HTML output contains all 7 section headers per D-08 (year overview, trajectories, adherence, events, improvements, correlations, goals)
    - Test 4: _medication_adherence(year) returns dict[med_name, {total_days, tracked_days, adherence_pct, monthly_breakdown}]
    - Test 5: _extract_events_from_daily_logs(year) returns list of dicts with date, type, keyword, context
    - Test 6: _improvement_areas(year) returns list of {metric, direction, delta_pct} for metrics that improved or worsened
    - Test 7: Sparse data handling -- report with 0 records still generates valid HTML with "insufficient data" messages
    - Test 8: person_id threaded to all sub-engines
  </behavior>
  <action>
    Create `skills/_shared/health_annual_report.py` with class `HealthAnnualReport` per D-07.

    **Constructor:** Accept `data_dir`, `person_id`, `workspace_root`, `memory_dir`, `year` (int, default current year). Instantiate `HealthChartEngine(data_dir=data_dir, person_id=person_id)`, `CrossSkillReader(data_dir=data_dir)`, `CorrelationEngine(data_dir=data_dir)`, `HealthMemoryWriter(workspace_root=workspace_root, memory_root=memory_dir, person_id=person_id)` per D-11.

    **generate(write=True):** Main entry point. Assembles all 7 sections per D-08:

    **Section 1 - Year Overview:** Count total records across all skills for the year. Count distinct tracking days. Count doctor visits (from appointments item or daily logs). Format as key stats summary.

    **Section 2 - Metric Trajectories:** Call `chart_engine.generate_blood_pressure_chart(365)`, `generate_blood_glucose_chart(365)`, `generate_weight_chart(365)`, `generate_sleep_chart(365)`. Base64-encode each for HTML embedding. Show all 12 months; empty periods visible as gaps (per Open Question 2 recommendation).

    **Section 3 - Medication Adherence:** Implement `_medication_adherence(year)` per research code example. Use `CrossSkillReader.read_medication_doses(start="{year}-01-01", end="{year}-12-31", person_id=self.person_id)`. Group by medication name, calculate days with records / total days as percentage. Build monthly breakdown heatmap-style visualization (e.g., colored cells in HTML table per month).

    **Section 4 - Health Events Timeline:** Implement `_extract_events_from_daily_logs(year)` per research code example. Use `_EVENT_KEYWORDS` list for keyword matching in daily log files (`daily_dir.glob(f"{year}-*.md")`). Extract date, event type, keyword, context line. Sort chronologically. Also check structured items (appointments.md, medications.md) for date-tagged changes.

    **Section 5 - Improvement Areas:** Implement `_improvement_areas(year)`. Compare first-quarter averages vs last-quarter averages for BP, glucose, weight, sleep. Calculate percentage change. Report which metrics improved (decreased BP = good, decreased weight = depends on goal) and which worsened. Be smart about direction: for BP/glucose, lower is better; for sleep duration, more is better.

    **Section 6 - Correlation Insights:** Call `CorrelationEngine.discover_correlations()` to get top correlations. Filter to `is_significant()`. Take top 3. Use `to_natural_language()` for Chinese insight text.

    **Section 7 - Goals Review:** Parse `_health-profile.md` for goals section. Compare stated goals vs actual metric progress during the year. E.g., goal "lower BP to 130/80" vs actual average BP in last 3 months.

    **HTML Template:** Use Jinja2 with inline template string `_ANNUAL_REPORT_HTML` per D-09, Pitfall 6. Styling per D-10:
    - Light background (#fafafa), clear section headers with colored left borders
    - Data tables with alternating row colors (#f9f9f9)
    - Mobile-responsive (media query for max-width 600px)
    - Print-friendly CSS (@media print)
    - Font stack: -apple-system, "PingFang SC", "Hiragino Sans GB", sans-serif
    - Section dividers, chart images max-width 100%
    - Medication adherence as color-coded monthly grid (green = >80%, yellow = 50-80%, red = <50%)
    - Events timeline as chronological list with type icons/labels
    - Clean, modern, medical-professional aesthetic

    **Output:** Single self-contained HTML file per D-09. No external dependencies. Save to `memory/health/files/annual-report-{year}.html`. Return {"path": str, "content": str, "format": "html"}.

    **Sparse data handling:** If tracking started mid-year, state "数据从{month}月开始（共{N}个月）" prominently at top. Show 12-month charts with visible gaps. Sections with zero data show "本年度暂无相关记录" instead of empty content. Per Open Question 2.

    **Import pattern:** Same as visit summary -- `sys.path.insert(0, os.path.dirname(__file__))` then import peers.

    Create `tests/test_annual_report.py` with unit tests. Mock all engines. Test that HTML contains all 7 section headers. Test medication adherence calculation with mock dose data. Test event extraction with mock daily log content. Test improvement areas with mock metric data. Test empty data produces valid HTML.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && python -m pytest tests/test_annual_report.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class HealthAnnualReport" skills/_shared/health_annual_report.py
    - grep -q "def generate" skills/_shared/health_annual_report.py
    - grep -q "def _medication_adherence" skills/_shared/health_annual_report.py
    - grep -q "def _extract_events" skills/_shared/health_annual_report.py
    - grep -q "def _improvement_areas" skills/_shared/health_annual_report.py
    - grep -q "discover_correlations\|CorrelationEngine" skills/_shared/health_annual_report.py
    - grep -q "base64" skills/_shared/health_annual_report.py
    - grep -q "person_id" skills/_shared/health_annual_report.py
    - grep -q "Jinja2\|jinja2\|Template" skills/_shared/health_annual_report.py
    - grep -q "_EVENT_KEYWORDS" skills/_shared/health_annual_report.py
    - python -m pytest tests/test_annual_report.py -x exits 0
  </acceptance_criteria>
  <done>HealthAnnualReport class generates self-contained HTML annual report with all 7 sections per D-08. Charts embedded as base64. Medication adherence calculated. Events extracted from daily logs. Improvement areas identified. Correlation insights included. Goals reviewed. All tests pass. Sparse data handled gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Annual report CLI script + SKILL.md</name>
  <files>scripts/generate_annual_report.py, skills/health-annual-report/SKILL.md</files>
  <read_first>
    - scripts/generate_visit_briefing.py (existing CLI pattern)
    - skills/health-visit-summary/SKILL.md (if created by plan 01, use as SKILL.md format reference; otherwise use skills/blood-pressure-tracker/SKILL.md)
  </read_first>
  <action>
    **CLI script** `scripts/generate_annual_report.py` per D-12:
    - Follow exact pattern from `generate_visit_briefing.py`: shebang `#!/usr/bin/env python3`, docstring, `from __future__ import annotations`, sys.path setup with ROOT and SHARED_DIR.
    - argparse arguments:
      - `--data-dir` (default None)
      - `--workspace-root` (default None)
      - `--memory-dir` (default None)
      - `--person-id` (default None) per D-11
      - `--year` (default current year, type int) -- the year to report on
      - `--output` (optional output file path; if not specified, use default memory/health/files/ location)
    - Instantiate `HealthAnnualReport(data_dir=args.data_dir, person_id=args.person_id, workspace_root=args.workspace_root, memory_dir=args.memory_dir, year=args.year)`
    - Call `result = report.generate()`
    - Print result path to stdout.
    - Handle errors gracefully with try/except and clear error messages.

    **SKILL.md** at `skills/health-annual-report/SKILL.md` per D-12:
    - YAML frontmatter: name "health-annual-report", description "生成年度健康回顾报告：指标轨迹、用药依从性、健康事件时间线、改善领域和相关性洞察", version "1.0.0", user-invocable true, allowed-tools ["Edit", "Read", "Write", "Bash"]
    - Markdown body: Describe when to use (year-end health review, understanding tracking progress), what it produces (self-contained HTML report), how to invoke CLI, example sections.
    - Chinese user-facing content per project convention.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && python scripts/generate_annual_report.py --help && test -f skills/health-annual-report/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - python scripts/generate_annual_report.py --help exits 0 and shows --year, --person-id options
    - grep -q "argparse" scripts/generate_annual_report.py
    - grep -q "HealthAnnualReport" scripts/generate_annual_report.py
    - grep -q "person.id\|person_id" scripts/generate_annual_report.py
    - grep -q "year" scripts/generate_annual_report.py
    - test -f skills/health-annual-report/SKILL.md
    - grep -q "health-annual-report" skills/health-annual-report/SKILL.md
  </acceptance_criteria>
  <done>CLI script runs with --help, accepts --year, --person-id flags. SKILL.md exists with proper frontmatter for OpenClaw discovery.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_annual_report.py -x -v` -- all unit tests pass
2. `python scripts/generate_annual_report.py --help` -- shows all expected CLI options
3. `python scripts/generate_annual_report.py --year 2025` -- produces HTML output (may show sparse data sections, but must not crash)
4. `test -f skills/health-annual-report/SKILL.md` -- skill definition exists
</verification>

<success_criteria>
- HealthAnnualReport class in skills/_shared/health_annual_report.py generates self-contained HTML report
- Report contains all 7 sections per D-08: year overview, metric trajectories, medication adherence, events timeline, improvement areas, correlation insights, goals review
- HTML output is self-contained with inline CSS and base64 chart images per D-09
- Styling is clean, modern, medical-professional per D-10
- CLI script at scripts/generate_annual_report.py with argparse (--year, --person-id)
- SKILL.md at skills/health-annual-report/SKILL.md for OpenClaw discovery
- All tests pass
- Sparse data handled gracefully (no crashes, Chinese messages for missing data)
</success_criteria>

<output>
After completion, create `.planning/phases/04-clinical-output/04-02-SUMMARY.md`
</output>
