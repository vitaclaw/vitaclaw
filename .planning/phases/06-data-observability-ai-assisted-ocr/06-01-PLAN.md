---
phase: 06-data-observability-ai-assisted-ocr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/_shared/health_data_store.py
  - scripts/health_data_dashboard.py
  - skills/health-data-dashboard/SKILL.md
  - tests/test_health_data_stats.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]

must_haves:
  truths:
    - "Calling HealthDataStore('skill').stats() returns record_count, last_updated, earliest_record, latest_record, time_span_days"
    - "Calling HealthDataStore('skill').stats(person_id='mom') filters to that person's records only"
    - "Calling HealthDataStore.global_stats() returns aggregated stats for all skill data directories"
    - "Running scripts/health_data_dashboard.py outputs a markdown table with Skill, Records, Last Updated, Time Span columns"
  artifacts:
    - path: "skills/_shared/health_data_store.py"
      provides: "stats() instance method and global_stats() classmethod"
      contains: "def stats"
    - path: "scripts/health_data_dashboard.py"
      provides: "CLI dashboard with --person-id, --format, --sort-by flags"
      contains: "argparse"
    - path: "skills/health-data-dashboard/SKILL.md"
      provides: "AI-invocable skill definition for the dashboard"
      contains: "health-data-dashboard"
    - path: "tests/test_health_data_stats.py"
      provides: "Unit tests for stats(), global_stats(), and dashboard output"
      contains: "class.*Test"
  key_links:
    - from: "scripts/health_data_dashboard.py"
      to: "skills/_shared/health_data_store.py"
      via: "HealthDataStore.global_stats()"
      pattern: "global_stats"
    - from: "skills/_shared/health_data_store.py"
      to: "data/{skill-name}/records.jsonl"
      via: "JSONL line scanning for stats"
      pattern: "_iter_records\\|stats"
---

<objective>
Add data observability to HealthDataStore: a stats() instance method for per-skill metrics, a global_stats() classmethod for cross-skill overview, and a CLI dashboard script that renders a markdown table.

Purpose: Users and the AI agent can inspect data health at a glance -- how many records per metric, when last updated, and data freshness.
Output: stats()/global_stats() methods on HealthDataStore, CLI dashboard script, SKILL.md for AI invocation, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/_shared/health_data_store.py
@skills/_shared/cross_skill_reader.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From skills/_shared/health_data_store.py:
```python
class HealthDataStore:
    def __init__(self, skill_name: str, data_dir: str | None = None):
        self.skill_name = skill_name
        self.data_root = _resolve_data_root(data_dir)
        self.skill_dir = self.data_root / skill_name
        self.data_file = str(self.skill_dir / "records.jsonl")

    def _iter_records(self):
        """Yields each valid dict from JSONL file."""

    def _read_all_records(self) -> list[dict]:
        """Returns all records sorted by timestamp."""

    def query(self, record_type=None, start=None, end=None, person_id=None) -> list[dict]:
        """Query records with optional filters."""
```

Record envelope format:
```python
{
    "id": "bp_20240101T120000_abc12345",
    "type": "bp",
    "timestamp": "2024-01-01T12:00:00",
    "skill": "blood-pressure-tracker",
    "note": "",
    "data": {...},
    "person_id": "mom",  # absent = self
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stats() and global_stats() to HealthDataStore</name>
  <files>skills/_shared/health_data_store.py, tests/test_health_data_stats.py</files>
  <read_first>skills/_shared/health_data_store.py, tests/test_health_shared.py</read_first>
  <behavior>
    - Test: stats() on empty skill returns {"record_count": 0, "last_updated": None, "earliest_record": None, "latest_record": None, "time_span_days": 0}
    - Test: stats() on skill with 3 records returns correct count=3, earliest/latest timestamps, and time_span_days calculated as (latest - earliest).days
    - Test: stats(person_id="mom") returns count/timestamps for only "mom" records, ignoring "self" and other person_id records
    - Test: stats(person_id="self") returns count/timestamps for records without person_id field (absence = self)
    - Test: global_stats() scans all subdirectories under data_root that contain records.jsonl and returns dict keyed by skill name
    - Test: global_stats(person_id="mom") filters each skill's stats to that person_id
  </behavior>
  <action>
    1. Create tests/test_health_data_stats.py with HealthDataStatsTest(unittest.TestCase). Use tmp_path via setUp() to create isolated data dirs with synthetic JSONL records. Write tests for all behaviors above.

    2. Add `stats()` instance method to HealthDataStore (per D-01):
       - Iterate records via _iter_records() (not _read_all_records to avoid sort overhead)
       - Count records, track min/max timestamps, apply person_id filtering using same logic as query()
       - For speed: scan lines and parse only "timestamp" and "person_id" fields -- do NOT parse full record data (per specifics: "scan JSONL file line counts rather than parsing every record"). Use json.loads on each line but only access timestamp/person_id keys.
       - Return: {"record_count": int, "last_updated": str|None, "earliest_record": str|None, "latest_record": str|None, "time_span_days": int}
       - time_span_days = (latest_date - earliest_date).days, 0 if < 2 records

    3. Add `global_stats()` classmethod to HealthDataStore (per D-02):
       - Accepts optional data_dir and person_id parameters
       - Scans all subdirectories under data_root for records.jsonl
       - For each, creates HealthDataStore(skill_name, data_dir) and calls stats(person_id)
       - Returns: {"skills": {skill_name: stats_dict, ...}, "total_records": int, "skills_count": int}

    Use `from __future__ import annotations` at top. Follow existing code style (4-space indent, docstrings, type hints).
  </action>
  <verify>
    <automated>cd /Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main && python -m pytest tests/test_health_data_stats.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def stats" skills/_shared/health_data_store.py
    - grep -q "def global_stats" skills/_shared/health_data_store.py
    - grep -q "record_count" skills/_shared/health_data_store.py
    - grep -q "time_span_days" skills/_shared/health_data_store.py
    - grep -q "person_id" tests/test_health_data_stats.py
    - python -m pytest tests/test_health_data_stats.py -x exits 0
  </acceptance_criteria>
  <done>stats() returns per-skill record count, last update, earliest/latest, time span. global_stats() aggregates across all skills. Both support person_id filtering. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create CLI dashboard script and SKILL.md</name>
  <files>scripts/health_data_dashboard.py, skills/health-data-dashboard/SKILL.md</files>
  <read_first>skills/_shared/health_data_store.py, scripts/run_health_heartbeat.py</read_first>
  <action>
    1. Create scripts/health_data_dashboard.py (per D-03):
       - Shebang: #!/usr/bin/env python3
       - Module docstring describing the CLI dashboard
       - sys.path setup following existing pattern: sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "skills" / "_shared"))
       - Import HealthDataStore
       - argparse with: --person-id (optional), --format markdown|json (default: markdown), --sort-by records|updated|span (default: records)
       - Call HealthDataStore.global_stats(person_id=args.person_id)
       - Markdown output format (per D-03):
         ```
         # Health Data Dashboard

         | Skill | Records | Last Updated | Time Span |
         |-------|---------|--------------|-----------|
         | blood-pressure-tracker | 42 | 2024-03-15 | 90 days |
         ```
       - Sort by chosen field (records=record_count desc, updated=last_updated desc, span=time_span_days desc)
       - JSON output: json.dumps(global_stats_result, ensure_ascii=False, indent=2)
       - Empty state: print "No health data found." with helpful message
       - If --person-id provided, add "Person: {pid}" header line

    2. Create skills/health-data-dashboard/SKILL.md (per D-04):
       - YAML frontmatter: name, description, version: "1.0.0", user-invocable: true, allowed-tools: [execute_python]
       - Markdown body: brief description, usage examples, reference to scripts/health_data_dashboard.py
       - Chinese description for user-facing text: "查看健康数据概览：各项指标的记录数量、最后更新时间、数据时间跨度"

    3. Add a simple integration test at the bottom of tests/test_health_data_stats.py:
       - HealthDataDashboardTest class
       - Test: run dashboard script with --format json on a temp data dir, verify JSON output has expected structure
       - Use subprocess.run to invoke the script
  </action>
  <verify>
    <automated>cd /Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main && python scripts/health_data_dashboard.py --format json 2>/dev/null; python -m pytest tests/test_health_data_stats.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "argparse" scripts/health_data_dashboard.py
    - grep -q "global_stats" scripts/health_data_dashboard.py
    - grep -q "sort-by" scripts/health_data_dashboard.py
    - grep -q "person-id" scripts/health_data_dashboard.py
    - test -f skills/health-data-dashboard/SKILL.md
    - grep -q "health-data-dashboard" skills/health-data-dashboard/SKILL.md
    - python -m pytest tests/test_health_data_stats.py -x exits 0
  </acceptance_criteria>
  <done>CLI dashboard outputs markdown or JSON table of all tracked metrics sorted by chosen field. SKILL.md exposes dashboard for AI invocation. Integration test confirms script runs and produces valid output.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_health_data_stats.py -x -v (all stats and dashboard tests pass)
- python scripts/health_data_dashboard.py --format markdown (produces readable table or empty state)
- python scripts/health_data_dashboard.py --format json (produces valid JSON)
- grep -q "def stats" skills/_shared/health_data_store.py (method exists)
- grep -q "def global_stats" skills/_shared/health_data_store.py (classmethod exists)
</verification>

<success_criteria>
- HealthDataStore.stats() returns per-skill record counts, timestamps, and time span with person_id filtering
- HealthDataStore.global_stats() aggregates stats across all skill data directories
- CLI dashboard script outputs markdown/JSON with sorting and person_id filtering
- SKILL.md exists for AI invocation
- All unit and integration tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-observability-ai-assisted-ocr/06-01-SUMMARY.md`
</output>
