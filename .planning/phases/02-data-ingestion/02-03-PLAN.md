---
phase: 02-data-ingestion
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - scripts/import_huawei_health_export.py
  - scripts/import_xiaomi_health_export.py
  - skills/huawei-health-import/SKILL.md
  - skills/xiaomi-health-import/SKILL.md
  - tests/test_huawei_import.py
  - tests/test_xiaomi_import.py
autonomous: true
requirements: [IMPT-02, IMPT-03]

must_haves:
  truths:
    - "User can import Huawei Health data from ZIP export and see records in health store"
    - "User can import Xiaomi/Mi Fitness data from ZIP export and see records in health store"
    - "Both importers use HealthImporterBase with fuzzy dedup to prevent duplicates"
  artifacts:
    - path: "scripts/import_huawei_health_export.py"
      provides: "Huawei Health importer CLI"
      contains: "class HuaweiHealthImporter"
    - path: "scripts/import_xiaomi_health_export.py"
      provides: "Xiaomi/Mi Fitness importer CLI"
      contains: "class XiaomiHealthImporter"
    - path: "skills/huawei-health-import/SKILL.md"
      provides: "Huawei import skill"
      contains: "huawei-health-import"
    - path: "skills/xiaomi-health-import/SKILL.md"
      provides: "Xiaomi import skill"
      contains: "xiaomi-health-import"
  key_links:
    - from: "scripts/import_huawei_health_export.py"
      to: "skills/_shared/health_importer_base.py"
      via: "HuaweiHealthImporter inherits HealthImporterBase"
      pattern: "HealthImporterBase"
    - from: "scripts/import_xiaomi_health_export.py"
      to: "skills/_shared/health_importer_base.py"
      via: "XiaomiHealthImporter inherits HealthImporterBase"
      pattern: "HealthImporterBase"
---

<objective>
Implement Huawei Health and Xiaomi/Mi Fitness importers using the HealthImporterBase class from Plan 02. Both importers follow the same pattern: parse platform-specific export ZIP, map records to HealthDataStore JSONL format, apply fuzzy dedup.

Purpose: Complete the device import channel by supporting the three major wearable platforms used in China.
Output: Two importers with CLI scripts, SKILL.md files, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-ingestion/02-CONTEXT.md
@.planning/phases/02-data-ingestion/02-RESEARCH.md
@.planning/phases/02-data-ingestion/02-02-SUMMARY.md (HealthImporterBase and Google Fit importer)

@skills/_shared/health_importer_base.py (base class to inherit)
@scripts/import_google_fit_export.py (reference importer CLI pattern)
@skills/google-fit-import/SKILL.md (reference SKILL.md pattern)
</context>

<interfaces>
<!-- Key types and contracts from Plan 02 -->

From skills/_shared/health_importer_base.py (created in Plan 02):
```python
class HealthImporterBase:
    PLATFORM = "unknown"

    def __init__(self, data_dir=None, person_id=None): ...
    def import_export(self, export_path: str, start_date=None, end_date=None) -> dict: ...
    def _extract_zip(self, zip_path: str) -> Path: ...
    def _discover_files(self, extracted_dir: Path) -> list[Path]:  # MUST override
        raise NotImplementedError
    def _parse_records(self, data_file: Path) -> list[dict]:  # MUST override
        raise NotImplementedError
    def _fuzzy_dedup(self, new_record: dict, existing_records: list[dict]) -> bool: ...
    def _store_records(self, records: list[dict]) -> dict: ...
    def _normalize_timestamp(self, ts_str: str, tz_offset: str | None = None) -> str: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Huawei Health importer + CLI + SKILL.md</name>
  <files>scripts/import_huawei_health_export.py, skills/huawei-health-import/SKILL.md, tests/test_huawei_import.py</files>
  <read_first>
    - skills/_shared/health_importer_base.py (base class API)
    - scripts/import_google_fit_export.py (reference CLI pattern from Plan 02)
    - skills/google-fit-import/SKILL.md (reference SKILL.md)
    - .planning/phases/02-data-ingestion/02-CONTEXT.md (D-06, D-08, D-09)
    - .planning/phases/02-data-ingestion/02-RESEARCH.md (Huawei format details, Pitfall 2 on field discovery)
  </read_first>
  <behavior>
    - Test: HuaweiHealthImporter._discover_files finds CSV, JSON, GPX files in extracted Huawei export ZIP
    - Test: _parse_records parses Huawei heart rate CSV/JSON into heart_rate records
    - Test: _parse_records parses Huawei step count data into steps records
    - Test: _parse_records parses Huawei sleep data into sleep_session records
    - Test: _parse_records handles unexpected/missing columns gracefully (logs warning, skips)
    - Test: Full import_export with synthetic Huawei ZIP produces correct records
  </behavior>
  <action>
    Create `HuaweiHealthImporter(HealthImporterBase)` in `scripts/import_huawei_health_export.py` (self-contained like Apple Health pattern).

    Implementation details:
    - PLATFORM = "huawei-health"
    - Per D-09: Huawei export is ZIP with CSV/JSON + GPX files from Settings export
    - `_discover_files`: Walk extracted directory for CSV, JSON, and GPX files. Huawei export structure varies but typically has directories like `Motion path detail data/`, `Health data/`, `Sport Health Data/`
    - `_parse_records`: Parse defensively per Claude's discretion and Pitfall 2:
      - Discover column names from CSV headers, do NOT hardcode column indices
      - Map recognized columns to record types (heart rate, steps, sleep, weight, bp, activities)
      - Log warnings for unrecognized file formats or columns, skip gracefully
      - Handle both CSV and JSON data files (Huawei uses mixed formats)
      - GPX files: extract activity routes with timestamps and duration
    - Per D-08: Map to standard types: heart_rate, steps, sleep_session, weight, bp, activity
    - Timestamps: Huawei may use local time or epoch ms -- detect and normalize per Pitfall 4

    CLI script follows same argparse pattern as import_google_fit_export.py:
    - Positional: export_zip path
    - Optional: --person-id, --start-date, --end-date, --format markdown|json, --data-dir

    SKILL.md: name: huawei-health-import, user-invocable: true, allowed-tools: [Bash]
    - Instructions for AI to ask for Huawei export ZIP, run script, report results

    Tests: Create synthetic Huawei CSV fixtures in tmp directory, test parsing and full import flow.
  </action>
  <verify>
    <automated>cd /Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main && python -m pytest tests/test_huawei_import.py -x -p no:dash -p no:asyncio -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class HuaweiHealthImporter" scripts/import_huawei_health_export.py returns match
    - grep "HealthImporterBase" scripts/import_huawei_health_export.py returns match
    - grep "name: huawei-health-import" skills/huawei-health-import/SKILL.md returns match
    - All tests in test_huawei_import.py pass
    - python scripts/import_huawei_health_export.py --help exits 0
  </acceptance_criteria>
  <done>Huawei Health importer parses export ZIP defensively, maps to JSONL records with fuzzy dedup, CLI and SKILL.md work</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Xiaomi/Mi Fitness importer + CLI + SKILL.md</name>
  <files>scripts/import_xiaomi_health_export.py, skills/xiaomi-health-import/SKILL.md, tests/test_xiaomi_import.py</files>
  <read_first>
    - skills/_shared/health_importer_base.py (base class API)
    - scripts/import_huawei_health_export.py (just created in Task 1 -- same pattern)
    - .planning/phases/02-data-ingestion/02-CONTEXT.md (D-06, D-08, D-09)
    - .planning/phases/02-data-ingestion/02-RESEARCH.md (Xiaomi format details, defensive parsing)
  </read_first>
  <behavior>
    - Test: XiaomiHealthImporter._discover_files finds CSV files in extracted Xiaomi export ZIP
    - Test: _parse_records parses Xiaomi heart rate CSV into heart_rate records
    - Test: _parse_records parses Xiaomi step count CSV into steps records
    - Test: _parse_records parses Xiaomi sleep CSV into sleep_session records
    - Test: _parse_records handles missing/unexpected CSV columns gracefully
    - Test: Full import_export with synthetic Xiaomi ZIP produces correct records
  </behavior>
  <action>
    Create `XiaomiHealthImporter(HealthImporterBase)` in `scripts/import_xiaomi_health_export.py`.

    Implementation details:
    - PLATFORM = "xiaomi"
    - Per D-09: Xiaomi export is ZIP with CSV files from account.xiaomi.com
    - `_discover_files`: Find CSV files in extracted directory. Xiaomi export typically has files like `ACTIVITY_MINUTE_*.csv`, `HEARTRATE_AUTO_*.csv`, `SLEEP_*.csv`, `BODY_*.csv`
    - `_parse_records`: Parse defensively per Claude's discretion:
      - Discover column names from CSV headers, auto-detect column meanings
      - Map recognized data to standard types: heart_rate, steps, sleep_session, weight, activity
      - Log warnings for unrecognized CSV formats, skip gracefully
      - Handle both legacy Mi Fit and new Mi Fitness export formats (column names may differ)
    - Per D-08: Map to standard types with per-Pitfall-4 timestamp normalization
    - Xiaomi typically uses local time timestamps

    CLI script: Same argparse pattern as other importers.
    SKILL.md: name: xiaomi-health-import, user-invocable: true, allowed-tools: [Bash]
    Tests: Synthetic Xiaomi CSV fixtures, test parsing and full import.
  </action>
  <verify>
    <automated>cd /Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main && python -m pytest tests/test_xiaomi_import.py -x -p no:dash -p no:asyncio -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class XiaomiHealthImporter" scripts/import_xiaomi_health_export.py returns match
    - grep "HealthImporterBase" scripts/import_xiaomi_health_export.py returns match
    - grep "name: xiaomi-health-import" skills/xiaomi-health-import/SKILL.md returns match
    - All tests in test_xiaomi_import.py pass
    - python scripts/import_xiaomi_health_export.py --help exits 0
  </acceptance_criteria>
  <done>Xiaomi/Mi Fitness importer parses export ZIP defensively, maps to JSONL records with fuzzy dedup, CLI and SKILL.md work</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_huawei_import.py tests/test_xiaomi_import.py -x -p no:dash -p no:asyncio` all pass
- `python scripts/import_huawei_health_export.py --help` exits 0
- `python scripts/import_xiaomi_health_export.py --help` exits 0
- Both importers inherit from HealthImporterBase
</verification>

<success_criteria>
- Huawei and Xiaomi importers parse their platform-specific ZIP formats defensively
- Both use HealthImporterBase with fuzzy dedup, preventing cross-device duplicates
- Unknown columns/formats are logged as warnings, not errors (per D-08)
- CLI scripts follow established argparse pattern
- SKILL.md files are discoverable and user-invocable
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-ingestion/02-03-SUMMARY.md`
</output>
