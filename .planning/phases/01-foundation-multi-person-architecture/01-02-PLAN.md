---
phase: 01-foundation-multi-person-architecture
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - skills/_shared/health_data_store.py
  - skills/_shared/health_memory.py
  - skills/_shared/cross_skill_reader.py
  - skills/_shared/family_manager.py
  - schemas/health-data-record.schema.json
  - tests/test_person_id_threading.py
autonomous: true
requirements:
  - ENG-05
  - FAM-01
  - FAM-02
  - FAM-03
  - FAM-04
  - FAM-05

must_haves:
  truths:
    - "User can create family member profiles via FamilyManager and they persist in _family.yaml"
    - "Recording data with person_id='mom' stores a record with person_id field in JSONL"
    - "Querying with person_id='mom' returns only mom's records, not self's"
    - "Querying with person_id=None returns ALL records (backward compat)"
    - "Querying with person_id='self' returns records with absent or 'self' person_id"
    - "Existing data without person_id field is treated as belonging to 'self' (zero-cost migration)"
    - "Memory items for 'self' use existing flat path (memory/health/items/blood-pressure.md)"
    - "Memory items for 'mom' use subdirectory path (memory/health/items/mom/blood-pressure.md)"
    - "CrossSkillReader.read() passes person_id through to HealthDataStore.query()"
    - "Dedup includes person_id -- same data for two different persons is NOT deduplicated"
  artifacts:
    - path: "skills/_shared/health_data_store.py"
      provides: "person_id parameter on append() and query()"
      contains: "person_id"
    - path: "skills/_shared/health_memory.py"
      provides: "person_id-aware memory path resolution"
      contains: "_resolve_items_path"
    - path: "skills/_shared/cross_skill_reader.py"
      provides: "person_id passthrough on read()"
      contains: "person_id"
    - path: "schemas/health-data-record.schema.json"
      provides: "person_id field in schema (optional)"
      contains: "person_id"
    - path: "tests/test_person_id_threading.py"
      provides: "Integration tests for person_id across all three modules"
      min_lines: 100
  key_links:
    - from: "skills/_shared/cross_skill_reader.py"
      to: "skills/_shared/health_data_store.py"
      via: "person_id parameter passthrough in read() -> query()"
      pattern: "person_id=person_id"
    - from: "skills/_shared/health_memory.py"
      to: "memory/health/items/"
      via: "_resolve_items_path with person_id subdirectory"
      pattern: "_resolve_items_path"
    - from: "skills/_shared/health_data_store.py"
      to: "data/*.jsonl"
      via: "person_id field written to and filtered from JSONL records"
      pattern: "person_id"
---

<objective>
Thread person_id through the entire data layer (HealthDataStore, HealthMemoryWriter, CrossSkillReader) and update the JSON schema, so every data operation can target a specific family member. Existing data without person_id is treated as "self" with zero-cost migration.

Purpose: Enable multi-person health tracking. Every subsequent feature phase gets per-person support for free.
Output: person_id-aware data layer with comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-multi-person-architecture/01-CONTEXT.md
@.planning/phases/01-foundation-multi-person-architecture/01-RESEARCH.md
@.planning/phases/01-foundation-multi-person-architecture/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01. Executor uses package imports. -->

From skills/_shared/health_data_store.py (current API to extend):
```python
class HealthDataStore:
    def __init__(self, skill_name: str, data_dir: str | None = None): ...
    def append(self, record_type: str, data: dict, note: str = "", timestamp=None, meta: dict | None = None, sign: bool = False) -> dict: ...
    def query(self, record_type: str | None = None, start=None, end=None) -> list[dict]: ...
```

From skills/_shared/health_memory.py (current API to extend):
```python
class HealthMemoryWriter:
    def __init__(self, workspace_root: str | None = None, memory_root: str | None = None, now_fn=None): ...
    # items_dir = self.base_dir / "items"
```

From skills/_shared/cross_skill_reader.py (current API to extend):
```python
class CrossSkillReader:
    def __init__(self, data_dir: str | None = None): ...
    def read(self, concept: str, start=None, end=None) -> list[dict]: ...
```

From skills/_shared/family_manager.py (existing, connects to data layer):
```python
class FamilyManager:
    def __init__(self, config_path=None, memory_dir=None, workspace_root=None, data_root=None, now_fn=None): ...
    # Stores profiles in memory/health/_family.yaml
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add person_id to HealthDataStore (append + query + dedup) and update schema</name>
  <files>
    skills/_shared/health_data_store.py
    schemas/health-data-record.schema.json
    tests/test_person_id_threading.py
  </files>
  <read_first>
    skills/_shared/health_data_store.py
    schemas/health-data-record.schema.json
    .planning/phases/01-foundation-multi-person-architecture/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test: append with person_id=None creates record WITHOUT person_id field (backward compat)
    - Test: append with person_id="self" creates record WITHOUT person_id field (self is implicit)
    - Test: append with person_id="mom" creates record WITH person_id="mom" field
    - Test: query with person_id=None returns ALL records regardless of person_id (backward compat)
    - Test: query with person_id="self" returns records where person_id is absent OR "self"
    - Test: query with person_id="mom" returns ONLY records with person_id="mom"
    - Test: dedup considers person_id -- same type+timestamp+data for person_id=None and person_id="mom" are NOT deduped
    - Test: dedup works within same person -- same type+timestamp+data+person_id="mom" IS deduped
    - Test: person_id slug validation -- rejects invalid slugs (uppercase, spaces, >32 chars)
  </behavior>
  <action>
1. Add `person_id: str | None = None` parameter to `HealthDataStore.append()` method. Per D-01, validate slug: kebab-case (`^[a-z0-9][a-z0-9-]*$`), max 32 chars. Raise `ValueError` on invalid slug.

2. In `append()`, normalize person_id: if `person_id` is None or "self", set `effective_pid = None`. Otherwise `effective_pid = person_id`. Only add `"person_id": person_id` to the record dict when `effective_pid is not None`. This implements D-06 zero-cost migration.

3. Update the dedup check in `append()` (around lines 183-195) to include person_id in comparison. The dedup key becomes `(type, timestamp, data_hash, effective_pid)`. Per Pitfall 5 from RESEARCH.md.

4. Add `person_id: str | None = None` parameter to `HealthDataStore.query()` method. Implement filtering per D-04:
   - `person_id=None`: no filter, return all records (backward compat)
   - `person_id="self"`: return records where `record.get("person_id")` is None or "self"
   - `person_id="mom"`: return records where `record.get("person_id") == "mom"`

5. Update `schemas/health-data-record.schema.json`: add `person_id` as an OPTIONAL field (do NOT add to `required` array):
```json
"person_id": {
  "type": "string",
  "pattern": "^[a-z0-9][a-z0-9-]*$",
  "maxLength": 32,
  "description": "Person slug. Absent or 'self' means the primary user."
}
```

6. Create `tests/test_person_id_threading.py` with tests for all behaviors listed above. Use `from skills._shared.health_data_store import HealthDataStore` (package import per Plan 01). Use tempdir for data_dir.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && pytest tests/test_person_id_threading.py -x -v && pytest tests/test_health_shared.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "person_id" skills/_shared/health_data_store.py` shows person_id in append() and query() signatures
    - `grep "person_id" schemas/health-data-record.schema.json` shows the person_id field definition
    - `grep -c "person_id" schemas/health-data-record.schema.json` returns at least 3 (field name, pattern, description)
    - `grep '"required"' schemas/health-data-record.schema.json` does NOT contain "person_id"
    - tests/test_person_id_threading.py contains at least 8 test methods
    - `pytest tests/test_person_id_threading.py -x` exits 0
    - `pytest tests/test_health_shared.py -x` exits 0 (existing tests unbroken)
  </acceptance_criteria>
  <done>HealthDataStore.append() and query() accept person_id parameter. Dedup includes person_id. Schema updated. All new and existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add person_id to HealthMemoryWriter and CrossSkillReader, verify FamilyManager integration</name>
  <files>
    skills/_shared/health_memory.py
    skills/_shared/cross_skill_reader.py
    skills/_shared/family_manager.py
    tests/test_person_id_threading.py
  </files>
  <read_first>
    skills/_shared/health_memory.py
    skills/_shared/cross_skill_reader.py
    skills/_shared/family_manager.py
    tests/test_person_id_threading.py
  </read_first>
  <behavior>
    - Test: HealthMemoryWriter with person_id=None resolves items path to `items/blood-pressure.md` (flat, backward compat)
    - Test: HealthMemoryWriter with person_id="self" resolves items path to `items/blood-pressure.md` (flat, same as None)
    - Test: HealthMemoryWriter with person_id="mom" resolves items path to `items/mom/blood-pressure.md`
    - Test: HealthMemoryWriter creates the person subdirectory if it does not exist
    - Test: CrossSkillReader.read() with person_id="mom" passes person_id to HealthDataStore.query() and returns only mom's records
    - Test: CrossSkillReader.read() without person_id returns all records (backward compat)
    - Test: FamilyManager can add a member and retrieve their profile
    - Test: Full integration -- FamilyManager adds "mom", data stored with person_id="mom", queried back filtered
  </behavior>
  <action>
1. Add `person_id: str | None = None` parameter to `HealthMemoryWriter.__init__()`. Store as `self._person_id = person_id`.

2. Create a centralized `_resolve_items_path(self, item_name: str) -> Path` method in HealthMemoryWriter (per Pitfall 3 from RESEARCH.md). Logic:
   - If `self._person_id` is None or "self": return `self.items_dir / f"{item_name}.md"` (existing flat path, per D-05/D-07)
   - Otherwise: return `self.items_dir / self._person_id / f"{item_name}.md"`
   - Ensure parent directory exists (call `path.parent.mkdir(parents=True, exist_ok=True)`)

3. Find ALL places in HealthMemoryWriter where item file paths are constructed (search for `self.items_dir /` patterns and any direct path construction like `f"{item_name}.md"`). Route ALL of them through `_resolve_items_path()`. The file is 1814 lines so be thorough -- grep for `items_dir` and `item_name` to find every site.

4. Add `person_id: str | None = None` parameter to `CrossSkillReader.read()` and `CrossSkillReader.read_all()`. Pass `person_id=person_id` to the `self._store(skill).query()` call.

5. Update ALL legacy convenience methods on CrossSkillReader (e.g., `read_blood_pressure`, `read_glucose_data`, etc.) to accept and pass through `person_id` parameter.

6. Verify FamilyManager already supports `add_member()` and `get_member()` per D-03. If not, add these methods. FamilyManager stores profiles in `memory/health/_family.yaml` with fields: `name`, `relationship`, `person_id` (slug), `demographics`. FamilyManager does NOT do data routing -- it is profile management only (per D-03).

7. Add tests to `tests/test_person_id_threading.py` for all the behaviors listed above. Include a full integration test that:
   a. Creates a FamilyManager, adds "mom"
   b. Creates a HealthDataStore, appends a record with person_id="mom"
   c. Queries with person_id="mom" -- gets the record
   d. Queries with person_id="self" -- does NOT get mom's record
   e. Creates a HealthMemoryWriter with person_id="mom"
   f. Verifies item path includes "mom" subdirectory
   g. Creates a CrossSkillReader, reads with person_id="mom"

8. Run the FULL test suite: `pytest tests/ -x` to ensure nothing is broken.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && pytest tests/test_person_id_threading.py -x -v && pytest tests/ -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "person_id" skills/_shared/health_memory.py` shows person_id in __init__ signature and _resolve_items_path method
    - `grep -n "_resolve_items_path" skills/_shared/health_memory.py` shows the method definition and at least 3 call sites
    - `grep -n "person_id" skills/_shared/cross_skill_reader.py` shows person_id in read() signature
    - `grep -n "person_id=person_id" skills/_shared/cross_skill_reader.py` shows passthrough to query()
    - tests/test_person_id_threading.py contains at least 15 test methods total (8 from Task 1 + 7+ from Task 2)
    - `pytest tests/test_person_id_threading.py -x` exits 0
    - `pytest tests/ -x` exits 0 (all tests pass including existing ones)
  </acceptance_criteria>
  <done>HealthMemoryWriter resolves per-person item paths. CrossSkillReader passes person_id through. FamilyManager verified. Full integration test passes. All existing tests unbroken.</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_person_id_threading.py -v` passes all person_id tests
2. `pytest tests/ -x` passes all existing tests (backward compat)
3. `python -c "from skills._shared.health_data_store import HealthDataStore; s = HealthDataStore('test'); help(s.append)"` shows person_id parameter
4. `python -c "from skills._shared.cross_skill_reader import CrossSkillReader; help(CrossSkillReader.read)"` shows person_id parameter
5. `grep -c 'person_id' schemas/health-data-record.schema.json` returns >= 3
</verification>

<success_criteria>
- HealthDataStore.append() accepts person_id, writes it to JSONL only when not "self"
- HealthDataStore.query() filters by person_id with correct None/self/other semantics
- Dedup includes person_id in comparison key
- HealthMemoryWriter resolves per-person item paths via centralized _resolve_items_path
- CrossSkillReader.read() and read_all() pass person_id through to query()
- Schema has optional person_id field
- All new person_id tests pass
- All existing tests pass unchanged (backward compatibility)
- Zero-cost migration: absence of person_id field = "self" (no file rewriting)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-multi-person-architecture/01-02-SUMMARY.md`
</output>
