---
phase: 01-foundation-multi-person-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - skills/__init__.py
  - skills/_shared/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - skills/_shared/cross_skill_reader.py
  - skills/_shared/apple_health_bridge.py
  - skills/_shared/health_visit_workflow.py
  - skills/_shared/health_reminder_center.py
  - skills/_shared/health_heartbeat.py
  - skills/_shared/health_flagship_scenarios.py
  - skills/_shared/health_operations.py
  - skills/_shared/health_team_runtime.py
  - skills/_shared/health_timeline_builder.py
  - skills/_shared/health_memory_distiller.py
  - skills/_shared/correlation_engine.py
  - skills/_shared/memory_editor.py
  - tests/test_health_shared.py
  - tests/test_apple_health_bridge.py
  - tests/test_diabetes_workflow.py
  - tests/test_doctor_matching.py
  - tests/test_flagship_scenarios.py
  - tests/test_health_heartbeat.py
  - tests/test_health_memory_distiller.py
  - tests/test_health_memory_golden.py
  - tests/test_health_operations.py
  - tests/test_health_reminder_center.py
  - tests/test_health_team_runtime.py
  - tests/test_health_timeline_builder.py
  - tests/test_health_visit_workflow.py
  - tests/test_hypertension_workflow.py
  - tests/test_init_health_workspace.py
  - tests/test_iteration2_active_service.py
  - tests/test_iteration3_safety_contracts.py
  - tests/test_medical_record_organizer_privacy.py
  - tests/test_monthly_digest_fallback.py
  - tests/test_patient_archive_bridge.py
  - tests/test_readme_render.py
  - tests/test_release_builder.py
  - tests/test_skill_governance.py
  - tests/test_smoke_test_skills.py
  - tests/test_weekly_digest_fallback.py
  - tests/test_annual_checkup_memory.py
autonomous: true
requirements:
  - ENG-01
  - ENG-02
  - ENG-04

must_haves:
  truths:
    - "Running `pip install -e .` installs VitaClaw with all core dependencies resolved from pyproject.toml"
    - "All shared modules can be imported via `from skills._shared.X import Y` without sys.path hacks"
    - "Running `ruff check skills/_shared/ tests/` passes with zero errors"
    - "Running `pytest tests/` discovers and passes all existing tests"
  artifacts:
    - path: "pyproject.toml"
      provides: "Build config, dependencies, ruff config, pytest config"
      contains: "[build-system]"
    - path: "skills/__init__.py"
      provides: "Makes skills a package"
    - path: "skills/_shared/__init__.py"
      provides: "Makes _shared importable as package"
    - path: "tests/__init__.py"
      provides: "Makes tests a package"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures"
  key_links:
    - from: "pyproject.toml"
      to: "skills/_shared/"
      via: "[tool.setuptools.packages.find] include = ['skills*']"
      pattern: "include.*skills"
    - from: "skills/_shared/cross_skill_reader.py"
      to: "skills/_shared/health_data_store.py"
      via: "relative import"
      pattern: "from \\.health_data_store import"
    - from: "tests/test_health_shared.py"
      to: "skills/_shared/"
      via: "package import (no sys.path)"
      pattern: "from skills._shared"
---

<objective>
Create pyproject.toml with setuptools backend, add __init__.py files to make skills/_shared/ importable as a proper Python package, convert all internal cross-imports in skills/_shared/ to relative imports, remove all sys.path.insert hacks from test files, configure ruff linter, and fix lint errors.

Purpose: Establish modern Python packaging so all subsequent work can use clean imports and automated quality gates.
Output: Working editable install, clean imports throughout, ruff passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-multi-person-architecture/01-CONTEXT.md
@.planning/phases/01-foundation-multi-person-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml, __init__.py files, and convert internal imports to relative</name>
  <files>
    pyproject.toml
    skills/__init__.py
    skills/_shared/__init__.py
    tests/__init__.py
    tests/conftest.py
    skills/_shared/cross_skill_reader.py
    skills/_shared/apple_health_bridge.py
    skills/_shared/health_visit_workflow.py
    skills/_shared/health_reminder_center.py
    skills/_shared/health_heartbeat.py
    skills/_shared/health_flagship_scenarios.py
    skills/_shared/health_operations.py
    skills/_shared/health_team_runtime.py
    skills/_shared/health_timeline_builder.py
    skills/_shared/health_memory_distiller.py
    skills/_shared/correlation_engine.py
    skills/_shared/memory_editor.py
  </files>
  <read_first>
    skills/_shared/cross_skill_reader.py
    skills/_shared/apple_health_bridge.py
    skills/_shared/health_visit_workflow.py
    skills/_shared/health_reminder_center.py
    skills/_shared/health_heartbeat.py
    skills/_shared/health_flagship_scenarios.py
    skills/_shared/health_operations.py
    skills/_shared/health_team_runtime.py
    skills/_shared/health_timeline_builder.py
    skills/_shared/health_memory_distiller.py
    skills/_shared/correlation_engine.py
    skills/_shared/memory_editor.py
    skills/_shared/health_data_store.py
    skills/_shared/health_memory.py
    skills/_shared/family_manager.py
    skills/_shared/concept_resolver.py
    skills/_shared/patient_archive_bridge.py
  </read_first>
  <action>
1. Create `pyproject.toml` at repo root with this exact content (per D-08, D-11):

```toml
[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vitaclaw"
version = "2.0.0"
description = "Modular health AI skill library for OpenClaw"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
viz = [
    "matplotlib>=3.10",
    "pandas>=2.2.3",
]
ocr = [
    "paddleocr>=3.4.0",
    "paddlepaddle>=3.0",
]
oncology = [
    "numpy>=1.26",
    "pandas>=2.2.3",
]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
]

[tool.setuptools.packages.find]
include = ["skills*"]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
```

2. Create empty `skills/__init__.py` (just a comment: `# VitaClaw skills package`)
3. Create empty `skills/_shared/__init__.py` (just a comment: `# VitaClaw shared runtime modules`)
4. Create empty `tests/__init__.py` (just a comment: `# VitaClaw test package`)
5. Create `tests/conftest.py` with a minimal fixture:
```python
"""Shared pytest fixtures for VitaClaw tests."""
from __future__ import annotations
```

6. Convert ALL internal cross-imports within `skills/_shared/` from bare module imports to relative imports. Find every occurrence of patterns like `from health_data_store import ...` or `from concept_resolver import ...` inside `skills/_shared/*.py` and change to relative: `from .health_data_store import ...`, `from .concept_resolver import ...`.

Known internal cross-imports to convert (grep for bare module imports in each file):
- `cross_skill_reader.py`: `from health_data_store import HealthDataStore` -> `from .health_data_store import HealthDataStore`
- `cross_skill_reader.py`: `from concept_resolver import ConceptResolver` -> `from .concept_resolver import ConceptResolver`
- `apple_health_bridge.py`: `from patient_archive_bridge import PatientArchiveBridge` -> `from .patient_archive_bridge import PatientArchiveBridge`
- `health_visit_workflow.py`: `from health_heartbeat import ...` -> `from .health_heartbeat import ...`
- `health_visit_workflow.py`: `from health_memory import ...` -> `from .health_memory import ...`
- `health_visit_workflow.py`: `from health_timeline_builder import ...` -> `from .health_timeline_builder import ...`
- `health_reminder_center.py`: `from health_memory import ...` -> `from .health_memory import ...`
- And ALL other bare intra-package imports found by reading each file.

IMPORTANT: Also check for `import X` style (not just `from X import Y`). Some files may use `import health_data_store` directly. Convert those to `from . import health_data_store` or the `from .X import Y` form.

IMPORTANT: Do NOT change stdlib imports (`import json`, `from pathlib import Path`, etc.) or third-party imports (`import yaml`, `import requests`). Only change imports that reference other files within `skills/_shared/`.

7. Run `pip install -e ".[dev]"` to install in editable mode.

8. Run `ruff check skills/_shared/ tests/` to see initial lint errors.

9. Fix all ruff errors. Use `ruff check --fix skills/_shared/ tests/` for auto-fixable issues. Manually fix any remaining errors. Common expected issues: unused imports (F401), undefined names (F821), import order (I001), bare except (E722).

IMPORTANT: Do NOT suppress errors with `# noqa` comments unless the error is a genuine false positive. Fix the root cause.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && pip install -e ".[dev]" && python -c "from skills._shared.health_data_store import HealthDataStore; print('OK')" && ruff check skills/_shared/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists and contains `[build-system]`, `requires = ["setuptools>=75.0"]`, `name = "vitaclaw"`, `version = "2.0.0"`, `requires-python = ">=3.11"`
    - pyproject.toml contains `[tool.ruff]` with `line-length = 120` and `target-version = "py311"`
    - pyproject.toml contains `[tool.ruff.lint]` with `select = ["E", "F", "I", "W", "UP"]`
    - pyproject.toml contains `[tool.pytest.ini_options]` with `testpaths = ["tests"]`
    - skills/__init__.py exists
    - skills/_shared/__init__.py exists
    - tests/__init__.py exists
    - tests/conftest.py exists
    - `grep -r "sys.path.insert" skills/_shared/` returns no results
    - `grep -rn "^from health_data_store import\|^from health_memory import\|^from concept_resolver import\|^from patient_archive_bridge import\|^from health_heartbeat import\|^from health_timeline_builder import\|^from health_reminder_center import" skills/_shared/` returns no results (all converted to relative)
    - `pip install -e ".[dev]"` exits 0
    - `python -c "from skills._shared.health_data_store import HealthDataStore; print('OK')"` prints OK
    - `ruff check skills/_shared/ tests/` exits 0
  </acceptance_criteria>
  <done>pyproject.toml created with all sections, __init__.py files exist, all internal imports in skills/_shared/ use relative form, ruff passes on skills/_shared/ and tests/</done>
</task>

<task type="auto">
  <name>Task 2: Remove sys.path.insert hacks from all test files and convert to package imports</name>
  <files>
    tests/test_health_shared.py
    tests/test_apple_health_bridge.py
    tests/test_diabetes_workflow.py
    tests/test_doctor_matching.py
    tests/test_flagship_scenarios.py
    tests/test_health_heartbeat.py
    tests/test_health_memory_distiller.py
    tests/test_health_memory_golden.py
    tests/test_health_operations.py
    tests/test_health_reminder_center.py
    tests/test_health_team_runtime.py
    tests/test_health_timeline_builder.py
    tests/test_health_visit_workflow.py
    tests/test_hypertension_workflow.py
    tests/test_init_health_workspace.py
    tests/test_iteration2_active_service.py
    tests/test_iteration3_safety_contracts.py
    tests/test_medical_record_organizer_privacy.py
    tests/test_monthly_digest_fallback.py
    tests/test_patient_archive_bridge.py
    tests/test_readme_render.py
    tests/test_release_builder.py
    tests/test_skill_governance.py
    tests/test_smoke_test_skills.py
    tests/test_weekly_digest_fallback.py
    tests/test_annual_checkup_memory.py
  </files>
  <read_first>
    tests/test_health_shared.py
    tests/test_flagship_scenarios.py
    tests/test_health_heartbeat.py
    tests/test_health_operations.py
    tests/test_health_team_runtime.py
    tests/test_health_visit_workflow.py
    tests/test_health_timeline_builder.py
    tests/test_health_memory_distiller.py
    tests/test_apple_health_bridge.py
    tests/test_diabetes_workflow.py
    tests/test_doctor_matching.py
    tests/test_health_memory_golden.py
    tests/test_health_reminder_center.py
    tests/test_hypertension_workflow.py
    tests/test_init_health_workspace.py
    tests/test_iteration2_active_service.py
    tests/test_iteration3_safety_contracts.py
    tests/test_medical_record_organizer_privacy.py
    tests/test_monthly_digest_fallback.py
    tests/test_patient_archive_bridge.py
    tests/test_readme_render.py
    tests/test_release_builder.py
    tests/test_skill_governance.py
    tests/test_smoke_test_skills.py
    tests/test_weekly_digest_fallback.py
    tests/test_annual_checkup_memory.py
  </read_first>
  <action>
For EVERY test file in `tests/`:

1. Remove the sys.path.insert block. The typical pattern to remove is:
```python
ROOT = Path(__file__).resolve().parents[1]
SHARED_DIR = ROOT / "skills" / "_shared"
sys.path.insert(0, str(SHARED_DIR))
```
Some files may also add `ROOT / "scripts"` or other paths. Remove ALL `sys.path.insert` calls.

2. Convert bare imports to package imports:
```python
# BEFORE:
from health_data_store import HealthDataStore  # noqa: E402
from health_memory import HealthMemoryWriter  # noqa: E402

# AFTER:
from skills._shared.health_data_store import HealthDataStore
from skills._shared.health_memory import HealthMemoryWriter
```

3. Remove all `# noqa: E402` comments that were suppressing the "module level import not at top of file" error -- these were only needed because imports came after sys.path manipulation. The imports should now be at the top of the file naturally.

4. If a test file imports from `scripts/` (e.g., `from build_vitaclaw_release import ...`), keep a minimal `sys.path.insert` ONLY for that script import, or skip that file if the script is not part of the skills._shared package. But first check if scripts/ modules can also be imported cleanly -- if not, it is acceptable to keep sys.path for scripts imports only.

5. Remove `import sys` if it was only used for `sys.path.insert` and is no longer needed.

6. Remove unused `ROOT` and `SHARED_DIR` variable definitions if they were only used for sys.path.

7. Keep any `ROOT` usage that is NOT for sys.path (e.g., `ROOT / "schemas"` for test data paths). In those cases, keep the `ROOT = Path(__file__).resolve().parents[1]` definition but remove the sys.path.insert line.

8. Run `ruff check tests/` and fix any new lint errors introduced by the refactoring.

9. Run `pytest tests/ -x` to verify all tests still pass. If any test fails due to import issues, debug and fix -- do NOT revert to sys.path hacks.

IMPORTANT: This migration must be atomic -- update ALL test files in a single pass (per Pitfall 6 from RESEARCH.md). Do not leave some files with old imports and some with new.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && grep -r "sys.path.insert" tests/ | grep -v "scripts" | wc -l && pytest tests/ -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "sys.path.insert.*_shared" tests/` returns no results
    - `grep -rn "# noqa: E402" tests/` returns no results (or only for non-shared imports)
    - `grep -rn "from skills._shared" tests/test_health_shared.py` shows at least 5 matches (confirming package imports)
    - `pytest tests/ -x --tb=short` exits 0 (all tests pass)
    - `ruff check tests/` exits 0
  </acceptance_criteria>
  <done>All 25+ test files use `from skills._shared.X import Y` package imports, no sys.path.insert for _shared, all tests pass, ruff clean</done>
</task>

</tasks>

<verification>
1. `pip install -e ".[dev]"` succeeds
2. `python -c "from skills._shared.health_data_store import HealthDataStore; from skills._shared.health_memory import HealthMemoryWriter; from skills._shared.cross_skill_reader import CrossSkillReader; print('All imports OK')"` prints "All imports OK"
3. `ruff check skills/_shared/ tests/` exits 0
4. `pytest tests/ -v` discovers and passes all existing tests
5. No `sys.path.insert` referencing `_shared` remains in tests/
</verification>

<success_criteria>
- pyproject.toml exists with correct build-system, dependencies, extras, ruff config, pytest config
- Editable install works: `pip install -e ".[dev]"` succeeds
- Package imports work: `from skills._shared.X import Y` for all shared modules
- All internal imports in skills/_shared/ use relative form
- All test files use package imports (no sys.path hacks for _shared)
- ruff passes on skills/_shared/ and tests/
- All existing tests pass under pytest
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-multi-person-architecture/01-01-SUMMARY.md`
</output>
