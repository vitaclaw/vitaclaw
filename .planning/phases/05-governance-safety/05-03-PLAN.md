---
phase: 05-governance-safety
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_safety_boundaries.py
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-03, SAFE-04]

must_haves:
  truths:
    - "Test catches output missing '必须就医' for red-flag vitals (systolic >180, glucose >20 mmol/L)"
    - "Test catches MEMORY.md auto-loading in group/public contexts"
    - "Test catches diagnostic conclusions in skill output (确诊, 诊断为, 你患有)"
    - "Test catches high-risk escalation paths that fail to interrupt normal flow"
  artifacts:
    - path: "tests/test_safety_boundaries.py"
      provides: "SOUL.md safety boundary test suite"
      contains: "必须就医"
      min_lines: 80
  key_links:
    - from: "tests/test_safety_boundaries.py"
      to: "skills/_shared/health_scenario_runtime.py"
      via: "imports HealthScenarioRuntime for output testing"
      pattern: "from skills._shared.health_scenario_runtime import"
    - from: "tests/test_safety_boundaries.py"
      to: "skills/_shared/health_memory.py"
      via: "imports HealthMemoryWriter for privacy testing"
      pattern: "from skills._shared.health_memory import"
    - from: "tests/test_safety_boundaries.py"
      to: "skills/_shared/health_heartbeat.py"
      via: "imports HealthHeartbeat for escalation testing"
      pattern: "from skills._shared.health_heartbeat import"
---

<objective>
Create a comprehensive safety boundary test suite that mechanically enforces SOUL.md rules.

Purpose: Ensure red-flag vitals always trigger "必须就医", diagnostic conclusions are never output, memory privacy is enforced in group contexts, and critical escalations interrupt normal flow.
Output: tests/test_safety_boundaries.py with tests for all 4 safety requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-governance-safety/05-CONTEXT.md

@SOUL.md
@skills/_shared/health_scenario_runtime.py
@skills/_shared/health_memory.py
@skills/_shared/health_heartbeat.py
@skills/_shared/health_team_runtime.py
@tests/test_iteration3_safety_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create safety boundary test suite for red-flag vitals and diagnostic prohibition</name>
  <files>tests/test_safety_boundaries.py</files>
  <read_first>
    - SOUL.md
    - skills/_shared/health_scenario_runtime.py
    - skills/_shared/health_heartbeat.py
    - skills/_shared/health_memory.py
    - skills/_shared/health_team_runtime.py
    - tests/test_iteration3_safety_contracts.py (for existing patterns, avoid duplication)
    - tests/conftest.py
  </read_first>
  <action>
Create `tests/test_safety_boundaries.py` per D-10 through D-14:

**Class `SafetyBoundariesTest(unittest.TestCase)`:**

**SAFE-01 — Red-flag vital signs (D-11):**
1. `test_extreme_systolic_bp_triggers_must_see_doctor`: Create a HealthScenarioRuntime instance, call `render_output()` with sections that include extreme blood pressure data (systolic >180 mmHg). Verify the output contains "必须就医" section. Use clinical thresholds per Claude's Discretion.
2. `test_extreme_glucose_triggers_must_see_doctor`: Same pattern with fasting glucose >20 mmol/L (or >360 mg/dL).
3. `test_extreme_heart_rate_triggers_must_see_doctor`: Resting HR >120 bpm.
4. `test_normal_vitals_no_forced_must_see_doctor`: Normal values should NOT force a "必须就医" section (the section exists but contains "暂无").

**SAFE-02 — Memory privacy in group/public (D-12):**
5. `test_group_context_blocks_memory_md_autoload`: Create a HealthTeamOrchestrator with `context="group"`, verify `role_policies()` shows `long_term_memory_allowed == False` for non-privileged roles.
6. `test_public_context_blocks_memory_md_autoload`: Same with `context="public"`.

**SAFE-03 — Diagnostic prohibition (D-13):**
7. `test_output_never_contains_diagnostic_patterns`: Generate output from HealthScenarioRuntime with various health data. Scan output for prohibited patterns: "确诊", "诊断为", "你患有", "诊断结论". Assert none found.
8. `test_diagnostic_prohibition_patterns_list`: Maintain a DIAGNOSTIC_PATTERNS constant list and verify it covers at least the patterns from SOUL.md.

**SAFE-04 — High-risk escalation (D-14):**
9. `test_heartbeat_flags_critical_vitals_high_priority`: Create a HealthHeartbeat instance with test data containing critical vital signs (e.g., systolic >180). Run `check_vitals()` or equivalent method. Verify returned issues include at least one with `priority == "high"`.
10. `test_critical_escalation_interrupts_normal_flow`: Verify that when critical vitals are present, the heartbeat output places them before routine advice (high-priority issues appear first in the issues list).

**Implementation notes:**
- Use `tempfile.TemporaryDirectory()` for all file-system-dependent tests
- Use `sys.path.insert` to add `skills/_shared` to path (following project pattern)
- Follow `unittest.TestCase` pattern per project conventions
- Use real clinical thresholds per Claude's Discretion (hypertensive crisis >180 systolic, diabetic emergency >20 mmol/L glucose, tachycardia >120 bpm resting)
- Do NOT duplicate tests from `test_iteration3_safety_contracts.py` -- that file tests team orchestration; this file tests shared runtime safety boundaries
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && python -m pytest tests/test_safety_boundaries.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - test -f tests/test_safety_boundaries.py
    - grep -q "必须就医" tests/test_safety_boundaries.py
    - grep -q "test_extreme_systolic_bp_triggers_must_see_doctor" tests/test_safety_boundaries.py
    - grep -q "test_group_context_blocks_memory_md_autoload" tests/test_safety_boundaries.py
    - grep -q "确诊" tests/test_safety_boundaries.py
    - grep -q "test_heartbeat_flags_critical_vitals_high_priority" tests/test_safety_boundaries.py
    - python -m pytest tests/test_safety_boundaries.py -x 2>&1 | grep -q "passed"
  </acceptance_criteria>
  <done>All safety boundary tests pass: red-flag vitals trigger "必须就医" (SAFE-01), group/public contexts block MEMORY.md auto-load (SAFE-02), no diagnostic conclusions in output (SAFE-03), critical vitals escalate as high-priority (SAFE-04).</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_safety_boundaries.py -v` shows all 10 tests passing
- Tests cover all 4 SAFE requirements with at least 2 test cases each
- No overlap with existing test_iteration3_safety_contracts.py
- Tests use real clinical thresholds, not arbitrary numbers
</verification>

<success_criteria>
- Red-flag vitals always produce "必须就医" section (SAFE-01)
- Group/public contexts block MEMORY.md auto-loading (SAFE-02)
- No diagnostic conclusion patterns in output (SAFE-03)
- Critical vitals trigger high-priority escalation (SAFE-04)
- All tests pass deterministically
</success_criteria>

<output>
After completion, create `.planning/phases/05-governance-safety/05-03-SUMMARY.md`
</output>
