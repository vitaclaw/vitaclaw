---
phase: 05-governance-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate_skill_frontmatter.py
  - tests/test_skill_linter.py
autonomous: true
requirements: [GOV-01, GOV-02]

must_haves:
  truths:
    - "Running the linter on any SKILL.md reports whether its YAML frontmatter is valid against skill-frontmatter.schema.json"
    - "Running the linter detects import direction violations where _shared/ imports from individual skills"
    - "Linter output includes remediation instructions for each violation"
    - "Linter runs as both standalone script and pytest test for CI"
  artifacts:
    - path: "scripts/validate_skill_frontmatter.py"
      provides: "Extended linter with import direction validation"
      contains: "import_direction"
    - path: "tests/test_skill_linter.py"
      provides: "Pytest wrapper for linter + unit tests for import direction checks"
      contains: "test_import_direction"
  key_links:
    - from: "scripts/validate_skill_frontmatter.py"
      to: "scripts/skill_catalog.py"
      via: "build_manifest_records import"
      pattern: "from skill_catalog import"
    - from: "tests/test_skill_linter.py"
      to: "scripts/validate_skill_frontmatter.py"
      via: "imports linter functions"
      pattern: "import.*validate_skill_frontmatter"
---

<objective>
Extend the existing skill structure linter to validate YAML frontmatter and import directions, with pytest integration for CI.

Purpose: Mechanical enforcement of skill structure rules that were previously documented but not checked.
Output: Enhanced linter script + pytest test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-governance-safety/05-CONTEXT.md

@scripts/validate_skill_frontmatter.py
@scripts/skill_catalog.py
@schemas/skill-frontmatter.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import direction validation to skill linter</name>
  <files>scripts/validate_skill_frontmatter.py</files>
  <read_first>
    - scripts/validate_skill_frontmatter.py
    - scripts/skill_catalog.py
    - schemas/skill-frontmatter.schema.json
    - skills/_shared/health_data_store.py (first 20 lines, for import pattern reference)
  </read_first>
  <action>
Extend `scripts/validate_skill_frontmatter.py` per D-01, D-02, D-03, D-04:

1. Add a new function `check_import_directions(skill_dir: Path) -> list[dict]` that:
   - Scans all `.py` files under `skills/_shared/` for import statements
   - Detects any import that references a specific skill directory (e.g., `from skills.blood_pressure_tracker import ...` or `sys.path` additions pointing to a specific skill)
   - Returns a list of violation dicts: `{"file": str, "line": int, "import_text": str, "violation": str, "remediation": str}`
   - Per D-03: `skills/{name}/` may import from `skills/_shared/`, but `skills/_shared/` must NOT import from any specific skill directory

2. Add a new function `check_skill_imports(skill_dir: Path) -> list[dict]` that:
   - Scans `.py` files in a specific skill directory
   - Verifies imports from `_shared` are valid (files exist)
   - Does NOT flag imports from `_shared` as violations (these are allowed)

3. Add `--check-imports` flag to the CLI that enables import direction validation
4. Add `--format` flag with choices `json` and `markdown` (per established CLI pattern)
5. When `--format markdown` is used, output a human-readable report with remediation instructions per D-02
6. Each error message must include remediation instructions (per D-02, Harness Engineering pattern: "error messages become agent context")
7. Make the import direction check also run when `--fail-on-error` is set

Keep all existing functionality intact. The script must remain backward-compatible.
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && python scripts/validate_skill_frontmatter.py --check-imports --format markdown 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "check_import_directions" scripts/validate_skill_frontmatter.py
    - grep -q "remediation" scripts/validate_skill_frontmatter.py
    - grep -q "check-imports" scripts/validate_skill_frontmatter.py
    - grep -q "format.*markdown" scripts/validate_skill_frontmatter.py
    - python scripts/validate_skill_frontmatter.py --help 2>&1 | grep -q "check-imports"
  </acceptance_criteria>
  <done>Linter validates frontmatter against schema and detects import direction violations with remediation instructions. Both --check-imports and --format flags work.</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest wrapper for linter with CI integration</name>
  <files>tests/test_skill_linter.py</files>
  <read_first>
    - scripts/validate_skill_frontmatter.py (updated version from Task 1)
    - tests/test_skill_governance.py (for test pattern reference)
    - tests/conftest.py
  </read_first>
  <action>
Create `tests/test_skill_linter.py` per D-02 (linter runs as pytest test so CI catches violations):

1. Class `SkillLinterTest(unittest.TestCase)`:
   - `test_all_skills_have_valid_frontmatter`: Runs `build_manifest_records()` from skill_catalog, asserts all health-core skills have `frontmatter_valid == True`. Collects failures into a readable assertion message showing skill slug and specific errors.
   - `test_shared_does_not_import_individual_skills`: Calls `check_import_directions()` from the updated linter, asserts no violations found. If violations exist, assertion message lists each file, line, and remediation.
   - `test_frontmatter_schema_required_fields`: Uses a synthetic SKILL.md (tempdir) missing required fields, validates that `validate_frontmatter()` catches each missing field.
   - `test_import_direction_catches_violation`: Creates a synthetic Python file in a tempdir that simulates `_shared/` importing from a specific skill, verifies the checker catches it.
   - `test_linter_remediation_messages_present`: Runs linter on a known-bad synthetic skill, verifies every error dict has a non-empty `remediation` key.

2. Use `sys.path.insert` to add `scripts/` so imports work (following project pattern).
3. Follow unittest.TestCase pattern (per project conventions).
  </action>
  <verify>
    <automated>cd "/Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main" && python -m pytest tests/test_skill_linter.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "test_all_skills_have_valid_frontmatter" tests/test_skill_linter.py
    - grep -q "test_shared_does_not_import_individual_skills" tests/test_skill_linter.py
    - grep -q "test_import_direction_catches_violation" tests/test_skill_linter.py
    - grep -q "remediation" tests/test_skill_linter.py
    - python -m pytest tests/test_skill_linter.py -x 2>&1 | grep -q "passed"
  </acceptance_criteria>
  <done>All 5 pytest tests pass. CI can now catch frontmatter violations and import direction issues automatically.</done>
</task>

</tasks>

<verification>
- `python scripts/validate_skill_frontmatter.py --check-imports --format markdown` produces readable output with remediation instructions
- `python -m pytest tests/test_skill_linter.py -v` shows all tests passing
- `python scripts/validate_skill_frontmatter.py --check-imports --fail-on-error` exits 0 (no import direction violations in _shared)
</verification>

<success_criteria>
- Linter validates frontmatter against schema with remediation instructions (GOV-01)
- Linter detects import direction violations in _shared/ (GOV-02)
- Pytest wrapper enables CI enforcement
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-governance-safety/05-01-SUMMARY.md`
</output>
