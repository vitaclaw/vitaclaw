---
phase: 03-visualization-correlation-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/_shared/correlation_engine.py
  - skills/health-correlation-analyzer/SKILL.md
  - scripts/analyze_health_correlations.py
  - tests/test_correlation_engine.py
  - pyproject.toml
autonomous: true
requirements: [CORR-01, CORR-02, CORR-03]

must_haves:
  truths:
    - "User can request cross-metric correlation analysis and get results with r, p-value, and sample size"
    - "Correlations with p >= 0.05 are reported as not significant"
    - "Correlations with n < 14 are reported as insufficient data"
    - "Results include natural language insights in Chinese"
  artifacts:
    - path: "skills/_shared/correlation_engine.py"
      provides: "Enhanced correlation engine with scipy stats, p-values, NL insights"
      exports: ["CorrelationEngine", "CorrelationResult"]
      contains: "scipy.stats"
    - path: "skills/health-correlation-analyzer/SKILL.md"
      provides: "User-invocable skill for correlation analysis"
      contains: "health-correlation-analyzer"
    - path: "scripts/analyze_health_correlations.py"
      provides: "CLI entry point for correlation analysis"
      contains: "argparse"
    - path: "tests/test_correlation_engine.py"
      provides: "Unit tests for enhanced correlation engine"
      contains: "class CorrelationEngineTest"
  key_links:
    - from: "skills/_shared/correlation_engine.py"
      to: "skills/_shared/cross_skill_reader.py"
      via: "CrossSkillReader.read() for multi-metric data access"
      pattern: "CrossSkillReader"
    - from: "skills/_shared/correlation_engine.py"
      to: "scipy.stats"
      via: "pearsonr and spearmanr for statistical significance"
      pattern: "from scipy.stats import"
    - from: "scripts/analyze_health_correlations.py"
      to: "skills/_shared/correlation_engine.py"
      via: "import and invoke CorrelationEngine"
      pattern: "from.*correlation_engine.*import"
---

<objective>
Enhance the existing correlation engine with statistical rigor (scipy p-values, Spearman rank correlation, n>=14 minimum) and add natural language insight generation in Chinese.

Purpose: Enable users to discover cross-metric health patterns backed by proper statistical significance testing, replacing the current hand-rolled Pearson-only implementation that lacks p-values and has an insufficient n>=3 minimum.

Output: Enhanced `correlation_engine.py`, `health-correlation-analyzer` SKILL.md, CLI script, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visualization-correlation-analysis/03-CONTEXT.md
@.planning/phases/03-visualization-correlation-analysis/03-RESEARCH.md

@skills/_shared/correlation_engine.py
@skills/_shared/cross_skill_reader.py

<interfaces>
<!-- Existing correlation_engine.py to enhance in-place -->

From skills/_shared/correlation_engine.py:
```python
class CorrelationResult:
    def __init__(self, concept_a, concept_b, field_a, field_b,
                 correlation, sample_count, direction, strength): ...
    def to_dict(self) -> dict: ...

class CorrelationEngine:
    DEFAULT_PAIRS = [
        ("blood-pressure", "systolic", "sleep", "score"),
        ("blood-pressure", "systolic", "weight", "kg"),
        ("blood-pressure", "systolic", "caffeine", "mg"),
        ("blood-sugar", "value", "weight", "kg"),
        ("blood-sugar", "value", "sleep", "score"),
        ("sleep", "score", "caffeine", "mg"),
    ]
    def __init__(self, data_dir=None, now_fn=None): ...
    def _get_daily_values(self, concept, field, window_days=30) -> dict[str, float]: ...
    def pearson_correlation(self, x, y) -> float: ...
    def correlate(self, concept_a, field_a, concept_b, field_b, window_days=30) -> CorrelationResult: ...
    def discover_correlations(self, window_days=30, min_strength="weak", pairs=None) -> list[CorrelationResult]: ...
```

From skills/_shared/cross_skill_reader.py:
```python
class CrossSkillReader:
    def read(self, concept, start=None, end=None, person_id=None) -> list[dict]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance correlation engine with scipy stats, p-values, Spearman, and NL insights</name>
  <files>skills/_shared/correlation_engine.py, tests/test_correlation_engine.py, pyproject.toml</files>
  <read_first>
    - skills/_shared/correlation_engine.py (full file — this is the file being enhanced)
    - skills/_shared/cross_skill_reader.py (data access interface, read() method)
    - schemas/health-concepts.yaml (canonical field names for DEFAULT_PAIRS alignment)
    - pyproject.toml (to add scipy dependency)
    - .planning/phases/03-visualization-correlation-analysis/03-RESEARCH.md (Pattern 3, Pitfalls 4-5)
  </read_first>
  <behavior>
    - Test 1: correlate() returns p_value field in CorrelationResult
    - Test 2: correlate() returns method field ("pearson" or "spearman") in CorrelationResult
    - Test 3: correlate() with n < 14 returns method="insufficient_data" and p_value=1.0
    - Test 4: correlate() with perfectly correlated data returns p_value close to 0 and r close to 1.0
    - Test 5: correlate() with random/uncorrelated data returns p_value > 0.05
    - Test 6: is_significant() returns True when p < 0.05 AND n >= 14
    - Test 7: is_significant() returns False when p < 0.05 but n < 14
    - Test 8: to_natural_language() returns Chinese insight for significant correlation
    - Test 9: to_natural_language() returns "未发现显著相关性" for non-significant results
    - Test 10: to_dict() includes p_value, method, natural_language fields
    - Test 11: discover_correlations() filters by significance (p < 0.05) not just strength
    - Test 12: Hand-rolled pearson_correlation() still works as fallback when scipy unavailable
  </behavior>
  <action>
    **Step 1: Add scipy to pyproject.toml:**
    - Add `"scipy>=1.11"` to the `[project.optional-dependencies]` `viz` list (or create an `analysis` extra)
    - Also lower matplotlib minimum to `>=3.8` to match installed version (per Research open question 2)

    **Step 2: Enhance `CorrelationResult` class (per D-07, D-08, D-11):**
    - Add fields: `p_value: float = 1.0`, `method: str = "pearson"`, `natural_language: str = ""`
    - Add method `is_significant(self) -> bool`: returns `self.p_value < 0.05 and self.sample_count >= 14`
    - Add method `to_natural_language(self) -> str` (per D-09):
      - If not significant: `f"{self.concept_a} 与 {self.concept_b} 之间未发现显著相关性（p={self.p_value:.3f}, n={self.sample_count}）"`
      - If significant: `f"过去分析期间，{self.concept_a} 与 {self.concept_b} 存在{strength_zh}{direction_zh}（r={self.correlation:.3f}, p={self.p_value:.3f}, n={self.sample_count}）"`
      - Chinese strength labels: strong="强", moderate="中等", weak="弱"
      - Chinese direction labels: positive="正相关", negative="负相关"
    - Update `to_dict()` to include `p_value`, `method`, `natural_language`, `is_significant`

    **Step 3: Enhance `CorrelationEngine.correlate()` (per D-07, D-08):**
    - Change minimum from n < 3 to n < 14 (per D-08)
    - Import scipy.stats.pearsonr and spearmanr (with try/except for graceful fallback)
    - Compute both Pearson and Spearman correlations
    - Use the method with lower p-value (more significant)
    - If scipy unavailable, fall back to existing hand-rolled pearson_correlation() with p_value=None
    - Add `person_id` parameter to correlate() and discover_correlations() — pass through to _get_daily_values

    **Step 4: Add person_id support to _get_daily_values():**
    - Add `person_id: str | None = None` parameter
    - Pass person_id to `CrossSkillReader.read()` calls

    **Step 5: Update DEFAULT_PAIRS (per D-10, Pitfall 5):**
    - Align field names with health-concepts.yaml canonical names
    - Updated pairs (verify against health-concepts.yaml):
      - ("sleep", "total_min", "blood-sugar", "value") — sleep duration vs glucose
      - ("blood-pressure", "systolic", "sleep", "total_min") — BP vs sleep
      - ("weight", "kg", "exercise", "duration_min") — weight vs exercise (if concept exists)
      - ("caffeine", "mg", "sleep", "total_min") — caffeine vs sleep
      - ("blood-pressure", "systolic", "weight", "kg") — BP vs weight
    - Keep backward compat: old pairs still work if data uses old field names (alias resolution already in _get_daily_values)

    **Step 6: Update discover_correlations():**
    - Filter by significance (p < 0.05) in addition to strength
    - Add `person_id` parameter
    - Sort by absolute correlation value (existing behavior)

    **Keep backward compatibility:**
    - Existing `pearson_correlation()` method stays as-is (used as fallback)
    - Existing `to_dict()` keys preserved, new keys added
    - Existing `correlate()` signature preserved, new optional params added

    Create `tests/test_correlation_engine.py`:
    - Use `unittest.TestCase`
    - Mock `CrossSkillReader.read()` to return synthetic records with known correlation patterns
    - Test with perfectly correlated data (r~1.0, p~0.0)
    - Test with uncorrelated random data (p > 0.05)
    - Test with insufficient data (n < 14)
    - Test natural language output contains Chinese text
    - Test scipy fallback by mocking ImportError
  </action>
  <verify>
    <automated>cd /Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main && python -m pytest tests/test_correlation_engine.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "scipy.stats" skills/_shared/correlation_engine.py
    - grep -q "p_value" skills/_shared/correlation_engine.py
    - grep -q "spearmanr" skills/_shared/correlation_engine.py
    - grep -q "is_significant" skills/_shared/correlation_engine.py
    - grep -q "to_natural_language" skills/_shared/correlation_engine.py
    - grep -q "未发现显著相关性" skills/_shared/correlation_engine.py
    - grep -q "sample_count >= 14" skills/_shared/correlation_engine.py
    - grep -q "person_id" skills/_shared/correlation_engine.py
    - grep -q "scipy" pyproject.toml
    - grep -q "class CorrelationEngineTest" tests/test_correlation_engine.py
    - python -m pytest tests/test_correlation_engine.py -x exits 0
  </acceptance_criteria>
  <done>
    Correlation engine computes Pearson + Spearman with scipy p-values, enforces n>=14, generates Chinese NL insights, supports person_id, and has comprehensive tests. scipy added to pyproject.toml.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create health-correlation-analyzer SKILL.md and CLI script</name>
  <files>skills/health-correlation-analyzer/SKILL.md, scripts/analyze_health_correlations.py</files>
  <read_first>
    - skills/caffeine-tracker/SKILL.md (SKILL.md format reference)
    - scripts/run_health_heartbeat.py (CLI script pattern)
    - skills/_shared/correlation_engine.py (just enhanced in Task 1)
  </read_first>
  <action>
    **Create `skills/health-correlation-analyzer/SKILL.md` (per D-12):**
    - YAML frontmatter: name "health-correlation-analyzer", description "跨指标相关性分析", version "1.0.0", user-invocable true, allowed-tools ["Read"], metadata.category "health-analyzer", metadata.iteration 2
    - Prompt body in Chinese describing the skill:
      - What it does: analyzes cross-metric correlations to discover health patterns
      - Pre-defined pairs (per D-10): 睡眠↔血糖, 用药↔血压, 运动↔体重, 睡眠↔血压, 咖啡因↔睡眠
      - Custom pairs: user can request any two metrics
      - Output includes: correlation coefficient (r), p-value, sample size (n), direction, natural language insight
      - Significance threshold: p < 0.05 with n >= 14 minimum
      - Usage examples: "分析我的睡眠和血糖有没有关联", "看看咖啡因对睡眠质量的影响"
      - Instructs the agent to use CorrelationEngine from skills/_shared/correlation_engine.py

    **Create `scripts/analyze_health_correlations.py` (per D-13):**
    - `#!/usr/bin/env python3`
    - `from __future__ import annotations`
    - Module docstring
    - argparse with:
      - `--pairs` optional, comma-separated concept pairs like "sleep:total_min,blood-sugar:value"
      - `--days` type=int, default=90
      - `--person-id` default=None
      - `--data-dir` default=None
      - `--format` default="markdown", choices=["markdown", "json"]
      - `--min-strength` default="weak", choices=["weak", "moderate", "strong"]
    - If --pairs not specified, uses DEFAULT_PAIRS from CorrelationEngine
    - Calls `discover_correlations()` or `correlate()` based on input
    - **Markdown output (per D-11):**
      - Header: "# 健康指标相关性分析"
      - For each significant result: concept pair, r value, p-value, n, NL insight
      - For non-significant results: brief mention "未发现显著相关性"
    - **JSON output (per D-11):**
      - `json.dumps([result.to_dict() for result in results], ensure_ascii=False, indent=2)`
    - Error handling: if no data available, print warning to stderr
  </action>
  <verify>
    <automated>cd /Users/baozhiwei/Library/CloudStorage/坚果云-452858265@qq.com/我的坚果云/其他项目/vitaclaw-main && python scripts/analyze_health_correlations.py --help && test -f skills/health-correlation-analyzer/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f skills/health-correlation-analyzer/SKILL.md
    - grep -q "health-correlation-analyzer" skills/health-correlation-analyzer/SKILL.md
    - grep -q "user-invocable: true" skills/health-correlation-analyzer/SKILL.md
    - test -f scripts/analyze_health_correlations.py
    - grep -q "argparse" scripts/analyze_health_correlations.py
    - grep -q "CorrelationEngine" scripts/analyze_health_correlations.py
    - grep -q "discover_correlations" scripts/analyze_health_correlations.py
    - python scripts/analyze_health_correlations.py --help exits 0
  </acceptance_criteria>
  <done>
    health-correlation-analyzer SKILL.md exists with correct frontmatter and Chinese prompt. CLI script accepts --pairs, --days, --person-id, --data-dir, --format, --min-strength and produces markdown/JSON output.
  </done>
</task>

</tasks>

<verification>
- Correlation engine produces p-values via scipy.stats
- Minimum n=14 enforced — smaller samples return "insufficient data"
- Results with p >= 0.05 reported as "not significant"
- Natural language insights generated in Chinese
- CLI script runs with --help and produces correct output
- Unit tests pass: `python -m pytest tests/test_correlation_engine.py -x -v`
- Existing correlation engine API backward compatible
</verification>

<success_criteria>
- correlation_engine.py enhanced with scipy Pearson + Spearman, p-values, n>=14 minimum
- CorrelationResult has is_significant(), to_natural_language(), updated to_dict()
- Chinese NL insights match D-09 format
- DEFAULT_PAIRS aligned with health-concepts.yaml field names
- person_id threaded through correlation methods
- SKILL.md and CLI script functional
- scipy added to pyproject.toml
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualization-correlation-analysis/03-02-SUMMARY.md`
</output>
